#include "main.h"

/**
* main - entry point
*
* description: writes and prints multiples of 3 or 5 under 1024
*
* Return: Always 0 (success)
*/

int main(void)
{
	int sum, num;

	for (num = 0; num < 1024; num++)
	{
		if ((num % 3 == 0) || (num % 5 ==))
			sum += num;
	}
	printf("%d\n", sum);

	return (0);
}

/**
* return - exit point
*
* description: termination of the program
*/
