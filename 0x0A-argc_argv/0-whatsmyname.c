#include <stdio.h>

/**
* main - writes its name / an entry point
* @argc: argument count
* @argv: array of pointers to argument strings
* Return: 0 (success)
**/

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
